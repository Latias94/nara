[package]
name = "nara-public-prelude-fixture"
version = "0.0.0"
edition = "2024"
rust-version = "1.95"
publish = false

[workspace]
resolver = "3"

[dependencies]
nara = { path = "../../..", default-features = false, features = ["runtime-core", "tooling"] }
